{
  "name": "deeptag",
  "private": true,
  "version": "1.0.0",
  "description": "DeepTag — inspect and edit the metadata hiding inside your files. Monorepo containing the website (deployed on Vercel) and the portable desktop app (Electron), sharing one parsing engine and one design system.",
  "workspaces": [
    "shared",
    "website",
    "desktop-app"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace website",
    "dev:desktop": "npm run dev --workspace desktop-app",
    "build:web": "npm run build --workspace website",
    "build:desktop": "npm run build --workspace desktop-app",
    "package:desktop": "npm run package --workspace desktop-app"
  }
}
